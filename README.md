# Система рекомендаций для ПСБ

> **Рекомендательная система на основе поведенческих паттернов пользователей**  
> Рекомендует финансовые продукты (ипотека, кредитные карты и др.) на основе анализа графов поведения.

---

## Описание проекта

Система анализирует поведенческие паттерны пользователей в экосистеме ПСБ (маркетплейс, платежи, офферы) и рекомендует наиболее подходящие финансовые продукты. 

**Ключевая идея:** 
> *«Просмотр дрели → оплата сантехнику → поиск квартир» → **рекомендация ипотеки***

Система выявляет сложные последовательности действий пользователя и на их основе формирует персонализированные рекомендации с объяснениями.

---

## Цель проекта

Разработать систему рекомендаций, которая:

1. **Выявляет поведенческие паттерны** — находит сложные последовательности действий пользователя через анализ графов поведения
2. **Создает профили пользователей** — собирает информацию о поведении, тратах, интересах
3. **Анализирует графы через YandexGPT** — использует искусственный интеллект для анализа структуры графа поведения, выявления значимых паттернов и генерации правил рекомендаций
4. **Прогнозирует потребности** — предсказывает, какие финансовые продукты подходят пользователю
5. **Обеспечивает интерпретируемость** — объясняет, почему рекомендован тот или иной продукт
6. **Готова к интеграции** — работает локально, может быть развернута в Docker

---

## Технологический стек

| Компонент | Технология | Назначение |
|-----------|------------|------------|
| **Обработка данных** | Polars | Быстрое чтение больших объемов данных из облака с predicate pushdown оптимизацией |
| **Граф поведения** | NetworkX | Построение и анализ графов взаимодействий пользователя |
| **Численные вычисления** | NumPy | Математические операции и нормализация признаков |
| **Анализ паттернов** | Встроенные инструменты Python | Выделение последовательностей действий и частых паттернов |
| **Анализ графов** | YandexGPT API | Анализ структуры графа поведения, выявление паттернов, генерация правил рекомендаций |
| **Модель рекомендаций** | RandomForest (scikit-learn) | Ранжирование продуктов по профилю пользователя |
| **Сохранение моделей** | Joblib | Сериализация и загрузка обученных моделей |
| **Объяснения** | YandexGPT API | Генерация понятных объяснений рекомендаций |
| **Демо-интерфейс** | Streamlit | Веб-интерфейс для работы с системой |
| **Визуализация** | Pyvis | Интерактивные графы поведения |
| **HTTP запросы** | Requests | Загрузка данных из Яндекс Диска и API запросы |
| **AI API** | OpenAI SDK | Работа с YandexGPT через OpenAI-compatible API |

> **YandexGPT используется для:**
> - **Анализа графов поведения** — анализ структуры графа, выявление значимых паттернов, генерация правил рекомендаций
> - **Генерации объяснений** — объяснение рекомендаций на основе профиля пользователя
> 
> На вход YandexGPT подаются только агрегаты (регион, категории, суммы, описание графа) — без сырых событий.

---

## Архитектура системы

Система построена по модульной архитектуре с четким разделением ответственности:

```
src/
├── main.py                    # Главный модуль - оркестрация всех компонентов
├── data/                      # Модули работы с данными
│   ├── cloud_loader.py       # Загрузка из Яндекс Диска с кэшированием и оптимизацией
│   ├── data_parser.py        # Нормализация данных к единому формату
│   └── loader.py             # Загрузка локальных данных (fallback)
├── features/                  # Модули извлечения признаков
│   ├── graph_builder.py      # Построение графов поведения (NetworkX)
│   ├── graph_analyzer.py     # Анализ графов через YandexGPT
│   ├── pattern_miner.py       # Извлечение паттернов поведения
│   ├── user_profile.py       # Создание профилей пользователей
│   └── rule_generator.py     # Генерация правил через YandexGPT
├── modeling/                  # Модули моделирования
│   ├── nbo_model.py          # ML модель (RandomForest) + улучшенный fallback
│   └── rule_engine.py        # Движок правил для рекомендаций
├── app/                       # Веб-приложение
│   ├── main.py               # Streamlit интерфейс с визуализацией
│   └── explainer.py          # Генерация объяснений (YandexGPT + fallback)
└── utils/                     # Утилиты
    ├── yandex_gpt_client.py   # Клиент для YandexGPT API
    ├── yandex_cloud.py        # Конфигурация Yandex Cloud
    └── user_finder.py         # Поиск пользователей в данных
```

### Детальное описание модулей:

#### Модули данных (`src/data/`):
- **`cloud_loader.py`** 
  - Загрузка данных из Яндекс Диска с кэшированием
  - Оптимизация: predicate pushdown для фильтрации по user_id ДО загрузки
  - Параллельная загрузка из кэша (до 8 потоков)
  - Загрузка справочников: brands.pq, users.pq, items.pq
  - Извлечение категорий брендов из каталогов товаров
  - **Оптимизация загрузки items.pq:**
    - Projection pushdown: загружаются только нужные колонки (item_id, brand_id, category, subcategory)
    - Predicate pushdown: фильтрация по brand_id и item_id ДО загрузки в память
    - LazyFrame: отложенная загрузка с агрегацией на уровне LazyFrame
    - Опциональная загрузка embedding только для товаров пользователя
    - Экономия памяти: ~99.9% (загружается только 1-5 МБ вместо 30 ГБ)
  
- **`data_parser.py`** 
  - Нормализация данных к единому формату для всех доменов
  - Автоматическое определение структуры данных
  - Обработка валют (данные в долларах)
  - Сохранение всех важных колонок согласно T-ECD
  
- **`loader.py`** 
  - Загрузка локальных данных (fallback для офлайн режима)

#### Модули анализа (`src/features/`):
- **`graph_builder.py`** 
  - Построение графов поведения через NetworkX
  - Оптимизация: агрегация по топ категориям/брендам
  - Временные связи между событиями
  
- **`graph_analyzer.py`** 
  - Анализ графов через YandexGPT (оптимизировано для экономии токенов)
  - Компактное описание графа (топ 15 узлов)
  - Использование названий брендов вместо ID
  
- **`pattern_miner.py`** 
  - Извлечение частых паттернов поведения
  - Анализ последовательностей действий
  
- **`user_profile.py`** 
  - Создание профилей пользователей
  - Статистика: платежи, просмотры, категории, бренды
  - Категории брендов из маппинга
  - **Использование embedding (опционально):**
    - Средний embedding товаров пользователя → представление интересов
    - Дисперсия embedding → разнообразие интересов
    - Embedding загружается только для товаров пользователя (экономия памяти)
  
- **`rule_generator.py`** 
  - Генерация правил через YandexGPT на основе паттернов

#### Модули моделирования (`src/modeling/`):
- **`nbo_model.py`** 
  - ML модель: RandomForestRegressor для ранжирования продуктов
  - Улучшенный fallback-алгоритм с нормализацией признаков
  - Комбинирование: базовые метрики (40%) + граф (35%) + паттерны (25%)
  - Использование категорий брендов для улучшения рекомендаций
  - Композитные индексы: high_value_customer, research_behavior, active_spender
  
- **`rule_engine.py`** 
  - Движок правил для рекомендаций на основе паттернов
  - Сопоставление паттернов с продуктами

#### Модули приложения (`src/app/`):
- **`main.py`** 
  - Streamlit веб-интерфейс
  - Визуализация графов через Pyvis
  - Отображение названий брендов и категорий
  - Fallback режим без YandexGPT
  
- **`explainer.py`** 
  - Генерация объяснений через YandexGPT
  - Fallback на шаблоны без YandexGPT
  - Использование категорий брендов в объяснениях

#### Утилиты (`src/utils/`):
- **`yandex_gpt_client.py`** 
  - Клиент для работы с YandexGPT через OpenAI-compatible API
  - Кэширование запросов
  
- **`yandex_cloud.py`** 
  - Конфигурация Yandex Cloud (API ключи, endpoints)
  
- **`user_finder.py`** 
  - Поиск пользователей в данных
  - Получение списка доступных пользователей

#### Основной модуль (`src/main.py`):
- **`process_user()`** 
  - Главная функция обработки пользователя
  - Интегрирует все компоненты: загрузку, графы, паттерны, профили, рекомендации
  - Загрузка справочников брендов и извлечение категорий из items.pq
  - Передача маппингов брендов во все компоненты

---

## Как это работает

### Шаг 1: Загрузка данных

Система загружает данные о поведении пользователей напрямую из облака (Яндекс Диск). Данные автоматически приводятся к единому формату, независимо от исходной структуры файлов.

### Шаг 2: Построение графа поведения

На основе событий пользователя (просмотры товаров, платежи) строится граф поведения. Граф показывает связи между различными действиями пользователя во времени.

### Шаг 3: Анализ графа через YandexGPT

YandexGPT анализирует структуру графа, выявляет значимые паттерны поведения и генерирует правила для рекомендаций продуктов. Например, если в графе видна последовательность "просмотр инструментов → оплата услуг → поиск недвижимости", система может рекомендовать ипотеку.

### Шаг 4: Извлечение паттернов

Система находит частые последовательности действий пользователя, такие как "просмотр → оплата → просмотр". Анализируются паттерны для выявления типичного поведения.

### Шаг 5: Создание профиля пользователя

Формируется профиль пользователя с информацией о:
- **Финансовых метриках**: средний чек, общая сумма, количество транзакций (в долларах)
- **Активности**: количество просмотров, дней активности, уникальных товаров
- **Интересах**: топ категории просмотров, топ бренды покупок
- **Категориях брендов**: извлеченные из каталогов товаров (`items.pq`) или справочника
- **Паттернах поведения**: частые последовательности действий
- **Embedding товаров (опционально)**: средний embedding товаров пользователя для понимания интересов

**Оптимизация загрузки каталогов товаров:**
- Загружаются только нужные колонки (без embedding по умолчанию)
- Фильтрация по brand_id и item_id пользователя ДО загрузки
- Embedding загружается опционально только для товаров пользователя
- Использование LazyFrame для отложенной загрузки и агрегации

### Шаг 6: Ранжирование продуктов

Модель машинного обучения (RandomForest) ранжирует финансовые продукты. Если модель не обучена, используется **улучшенный fallback-алгоритм**:

**Комбинирование источников:**
- Базовые метрики профиля (40%) — нормализованные значения
- Анализ графа (35%) — PageRank, пути, плотность
- Анализ паттернов (25%) — частоты событий, последовательности

**Использование категорий брендов:**
- Ипотека: категории недвижимости, ремонта, строительства
- Кредитная карта: розничная торговля, услуги, развлечения
- Вклад: финансовые услуги, инвестиции

### Шаг 7: Генерация объяснений

Для каждой рекомендации генерируется объяснение:
- **Через YandexGPT**: использует профиль, категории брендов, паттерны
- **Fallback**: шаблоны на основе метрик профиля

Объяснения учитывают категории брендов для более точных рекомендаций.

---

## Ключевые особенности

### Офлайн-работа
- Все вычисления выполняются локально
- YandexGPT вызывается для анализа графов и генерации объяснений (не в процессе работы модели)
- Модель обучена заранее
- Графы строятся локально, затем анализируются через YandexGPT

### Интерпретируемость
- Графы поведения визуализируются
- YandexGPT анализирует графы и генерирует правила рекомендаций
- Объяснения генерируются для каждой рекомендации
- Если YandexGPT недоступен, используются шаблоны объяснений

### Масштабируемость
- Эффективная работа с большими объемами данных
- Работа с данными из облака — не нужно скачивать 1+ ТБ данных локально
- Прямое чтение файлов через интернет
- **Оптимизация загрузки каталогов товаров:**
  - Projection pushdown: загружаются только нужные колонки (экономия ~95% размера)
  - Predicate pushdown: фильтрация ДО загрузки в память (экономия ~99.9%)
  - LazyFrame: агрегация на уровне LazyFrame без загрузки всего файла
  - Опциональная загрузка embedding только для товаров пользователя
  - Результат: загружается 1-5 МБ вместо 30 ГБ для items.pq

---

## Запуск в Docker

Система может быть запущена в Docker контейнере.

### Быстрый запуск

1. Откройте терминал в папке проекта `VIBE-TEAM`
2. Выполните команду:
   ```
   docker-compose up -d
   ```
3. Откройте браузер и перейдите по адресу: **http://localhost:8501**

### Управление контейнером

- **Запустить контейнер:** `docker-compose up -d`
- **Просмотреть логи:** `docker-compose logs -f`
- **Остановить контейнер:** `docker-compose down`
- **Пересобрать образ (только при изменении зависимостей):** `docker-compose build`
- **Пересобрать образ полностью (без кэша):** `docker-compose build --no-cache`

### Оптимизация сборки

Docker автоматически использует кэш для зависимостей. Зависимости переустанавливаются только при изменении файла `requirements.txt`. При изменении кода пересобирается только слой с кодом, зависимости берутся из кэша.

### Настройка

#### Переменные окружения

В файле `docker-compose.yml` можно настроить:

- `YANDEX_DISK_PUBLIC_LINK` - Публичная ссылка на Яндекс Диск с данными (по умолчанию уже настроена)
- `YANDEX_DISK_TOKEN` - Токен API Яндекс Диска (опционально, для приватных папок)

#### API ключи Yandex Cloud

API ключи Yandex Cloud встроены напрямую в код в файле `src/utils/yandex_cloud.py`. Дополнительная настройка не требуется.

### Решение проблем

**Порт 8501 уже занят:**

Измените порт в файле `docker-compose.yml` в разделе `ports` на другой, например `8502:8501`.

**Ошибка при загрузке данных из облака:**

1. Проверьте доступность Яндекс Диска
2. Убедитесь, что публичная ссылка корректна
3. Проверьте логи: `docker-compose logs vibe-team`

**Ошибка с API ключами Yandex Cloud:**

API ключи встроены в код. Если нужно изменить ключи, отредактируйте файл `src/utils/yandex_cloud.py`.

**Контейнер не запускается:**

1. Проверьте логи: `docker-compose logs vibe-team`
2. Попробуйте пересобрать образ: `docker-compose build --no-cache`
3. Перезапустите контейнер: `docker-compose down && docker-compose up -d --build`

**Ошибка "необходимо указать file_list":**

Если вы видите эту ошибку, значит в контейнере старая версия кода. Выполните:
```
docker-compose down
docker-compose build --no-cache
docker-compose up -d
```

---

## Важно: Доступ к данным

Система загружает данные напрямую из Яндекс Диска. Для работы с публичными папками **рекомендуется использовать Яндекс Диск API с токеном**, так как прямой доступ через публичные ссылки может быть ограничен (капча, HTML-страницы вместо файлов).

### Настройка пути к dataset

Если ваша папка с dataset находится не в корне Яндекс Диска, а в подпапке (например, `/Загрузки/Dataset_case_1`), необходимо указать базовый путь:

1. **Через переменную окружения в docker-compose.yml:**
   ```yaml
   environment:
     - YANDEX_DISK_BASE_PATH=Загрузки/Dataset_case_1
   ```

2. **Или через переменную окружения при запуске:**
   ```bash
   export YANDEX_DISK_BASE_PATH="Загрузки/Dataset_case_1"
   ```

**Примечание:** Базовый путь указывается без префикса `disk:` и без начального слеша. Например:
- ✅ Правильно: `Загрузки/Dataset_case_1`
- ✅ Правильно: `/Загрузки/Dataset_case_1` (начальный слеш будет автоматически убран)
- ❌ Неправильно: `disk:/Загрузки/Dataset_case_1`

Если dataset находится в корне диска, базовый путь можно не указывать.

### Получение токена Яндекс Диска API

OAuth credentials уже настроены в коде. Токен уже получен и встроен в `docker-compose.yml`.

Если нужно получить новый токен:

1. **Автоматический способ (рекомендуется):**
   - Запустите скрипт: `python scripts/get_yandex_disk_token.py`
   - Перейдите по ссылке и авторизуйтесь
   - Скопируйте код авторизации
   - Используйте скрипт `scripts/exchange_token_simple.py` для получения токена

2. **Ручной способ:**
   - Перейдите на https://oauth.yandex.ru/
   - Авторизуйтесь и получите токен
   - Добавьте токен через переменную окружения: `YANDEX_DISK_TOKEN=ваш_токен`

**Примечание:** OAuth credentials (ClientID и Client Secret) уже встроены в код. Токен уже получен и настроен в `docker-compose.yml`.

## Как использовать систему

### Получение списка пользователей для тестирования

**Вариант 1: Через веб-интерфейс (рекомендуется)**

1. Откройте веб-интерфейс по адресу http://localhost:8501
2. В боковой панели выберите вкладку "📋 Список"
3. Нажмите "🔄 Обновить список" для загрузки пользователей из данных
4. Выберите пользователя из списка

**Вариант 2: Через скрипт**

Запустите скрипт для получения списка пользователей:
```
python scripts/get_test_users.py
```

Скрипт выведет список доступных пользователей из данных.

### Поиск пользователя

**Важно:** Список пользователей загружается напрямую из облака (Яндекс Диск). Система автоматически находит всех пользователей в указанных файлах.

## Кэширование и оптимизация загрузки

### Как работает кэш

Система автоматически кэширует все загруженные файлы в директории `.cache/`. Это значительно ускоряет повторные запросы:

- **Первый запуск:** Файлы загружаются из облака и сохраняются в кэш
- **Последующие запуски:** Файлы загружаются из кэша (в 10-100 раз быстрее)
- **Проверка целостности:** Система автоматически проверяет валидность кэшированных файлов

### Оптимизация загрузки каталогов товаров (items.pq)

Система использует несколько техник оптимизации для работы с большими каталогами товаров (~30 ГБ):

**1. Projection Pushdown — только нужные колонки:**
- Загружаются только: `item_id`, `brand_id`, `category`, `subcategory`
- Embedding НЕ загружается по умолчанию (занимает ~95% размера файла)
- Embedding загружается опционально только для товаров пользователя

**2. Predicate Pushdown — фильтрация ДО загрузки:**
- Фильтрация по `brand_id` пользователя ДО загрузки в память
- Фильтрация по `item_id` пользователя ДО загрузки в память
- Используется `LazyFrame` для отложенной загрузки

**3. LazyFrame — отложенная загрузка:**
- Данные не загружаются в память до вызова `collect()`
- Все фильтры и агрегации выполняются на уровне LazyFrame
- Загрузка в память только после всех оптимизаций

**4. Агрегация на LazyFrame:**
- Для извлечения категорий брендов: группировка выполняется на LazyFrame
- В память загружаются только агрегированные результаты (brand_id → category)
- Экономия: вместо миллионов товаров загружаются только уникальные бренды

**Результат оптимизации:**

| Операция | Было | Стало | Экономия |
|----------|------|-------|----------|
| Загрузка items.pq | ~30 ГБ (весь файл) | ~1-5 МБ (только нужные товары) | ~99.9% |
| Память для категорий брендов | ~30 ГБ | ~1-10 МБ (только агрегаты) | ~99.9% |
| Время загрузки | Минуты | Секунды | ~95% |

**Использование embedding:**
- Embedding загружается опционально только для товаров пользователя
- Используется для вычисления среднего embedding (представление интересов)
- Используется для вычисления дисперсии embedding (разнообразие интересов)
- Учитывается в fallback-алгоритме для улучшения рекомендаций

### Режим приоритета кэша

Вы можете включить режим, когда система использует кэш как основной источник данных и обращается к облаку только если файла нет в кэше:

**В docker-compose.yml:**
```yaml
environment:
  - PREFER_CACHE=true
```

**Или через переменную окружения:**
```bash
export PREFER_CACHE=true
docker-compose up
```

**Преимущества:**
- Мгновенная загрузка из кэша (без проверки облака)
- Работа без интернета (если все файлы в кэше)
- Меньше нагрузка на Яндекс Диск API

**Недостатки:**
- Нужно вручную обновлять кэш при изменении данных в облаке

### Параллельная загрузка

Система автоматически использует параллельную загрузку для файлов из кэша (до 4 потоков одновременно), что значительно ускоряет обработку нескольких файлов.

### Как работает разбивка по датам

**Важно:** Согласно структуре датасета T-ECD (https://huggingface.co/datasets/t-tech/T-ECD), файлы партиционированы по дням: `{day}.pq`, где `{day}` - это номер дня (например, `01082.pq` - это день 1082).

**Логика работы:**
1. Файлы называются `01082.pq`, `01083.pq` - это номера дней (day numbers)
2. Каждый файл содержит данные за один день (daily partition)
3. Фильтрация по датам происходит **после загрузки** по колонке `timestamp` внутри файлов
4. Параметр `days=5` означает: загрузить файлы, затем отфильтровать события за последние 5 дней по `timestamp`

**Пример:**
- Загружаются файлы: `01082.pq`, `01083.pq`, `01084.pq` (дни 1082, 1083, 1084)
- Внутри каждого файла есть колонка `timestamp` с реальными датами событий
- Система фильтрует: `timestamp >= (сегодня - 5 дней)`
- В результате получаются только события за последние 5 дней

**Структура данных (согласно T-ECD):**
- **Marketplace events**: `item_id`, `brand_id`, `action_type`, `subdomain`, `price`, `count`, `os`, `timestamp`
- **Payments events**: `brand_id`, `action_type`, `price`, `timestamp` (нет `item_id`)
- **Retail events**: `item_id`, `brand_id`, `action_type`, `subdomain`, `price`, `count`, `os`, `timestamp`

**Важно:** У разных доменов разные схемы данных. Система автоматически обрабатывает это при объединении событий, выбирая только общие колонки (например, `timestamp` для временных характеристик).

**Рекомендация:** Если нужно загрузить данные за конкретный период, используйте параметр `days=N` при вызове `load_marketplace_events()` или `load_payments_events()`.

В веб-интерфейсе используйте вкладку "🔍 Поиск":
- Введите ID пользователя вручную
- Или используйте поиск по паттерну для поиска пользователей по части ID

**Откуда берутся ID пользователей:**
- Из файла `users.pq` в облаке (если доступен)
- Или из событий в файлах `marketplace/events/*.pq` в облаке
- Все ID автоматически конвертируются в строки для корректной работы

### Анализ пользователя

1. Выберите пользователя (см. выше)
2. Настройте параметры:
   - Использовать данные из облака (включено по умолчанию)
   - Использовать YandexGPT для анализа (включено по умолчанию)
   - Количество рекомендаций (от 1 до 10)
3. Нажмите кнопку "🔍 Анализировать"
4. Просмотрите результаты во вкладках:
   - **📊 Рекомендации** — топ продуктов с объяснениями
   - **📈 Статистика** — детальная информация о пользователе
   - **🕸️ Граф поведения** — визуализация поведения
   - **🔍 Паттерны** — найденные паттерны поведения

---

## Обучение модели с YandexGPT

### Предварительное обучение модели

Система поддерживает предварительное обучение модели с помощью YandexGPT для улучшения качества рекомендаций.

**Как обучить модель:**

**Важно:** 
- Скрипт обучения был добавлен недавно. Если вы получаете ошибку "No such file or directory", нужно пересобрать Docker образ.
- **Если вы изменили формат данных (например, перешли с рублей на доллары), модель нужно переобучить!** Старая модель была обучена на других признаках и будет давать некорректные результаты.

1. **Пересоберите Docker образ** (чтобы скрипт попал в образ):
   ```bash
   docker-compose build
   ```

2. **Убедитесь, что контейнер запущен:**
   ```bash
   docker-compose up -d
   ```
   
   Проверьте статус:
   ```bash
   docker-compose ps
   ```

3. **Запустите скрипт обучения через Docker:**
   
   **Вариант А: Если контейнер уже запущен (рекомендуется):**
   ```bash
   docker-compose exec vibe-team python scripts/train_model_with_yandexgpt.py
   ```
   
   **Вариант Б: Создать новый временный контейнер:**
   ```bash
   docker-compose run --rm vibe-team python scripts/train_model_with_yandexgpt.py
   ```
   
   **Примечание:** Если вы получаете ошибку "No such container", убедитесь, что:
   - Контейнер запущен: `docker-compose up -d`
   - Используете правильное имя сервиса: `vibe-team` (не `psb-recommendation`, это имя контейнера)

3. **Альтернатива: запустите локально** (если установлены зависимости):
   ```bash
   cd VIBE-TEAM
   python scripts/train_model_with_yandexgpt.py
   ```
   
   **Примечание:** При локальном запуске убедитесь, что:
   - Установлены все зависимости из `requirements.txt`
   - Настроены переменные окружения для YandexGPT и Yandex Disk

**Когда нужно переобучать модель:**
- После изменения формата данных (например, переход с рублей на доллары)
- После значительных изменений в структуре признаков
- При добавлении новых продуктов
- Для улучшения качества рекомендаций на новых данных

**Время обучения:**
- **Сбор профилей:** ~2-3 минуты (загрузка данных для 20 пользователей)
- **Генерация обучающих данных через YandexGPT:** ~5-8 минут (20-30 вызовов API)
- **Обучение модели:** ~5-10 секунд
- **Итого:** ~7-11 минут

**Сохранение модели:**
- ✅ Модель сохраняется в `./models/nbo_model.pkl` на хосте
- ✅ Модель доступна после перезапуска контейнера (благодаря volume `./models:/app/models`)
- ✅ Модель автоматически загружается при следующем запуске системы

**Устранение проблем при обучении:**
- **Ошибка "No such container":** 
  - Убедитесь, что контейнер запущен: `docker-compose up -d`
  - Проверьте статус: `docker-compose ps`
  - Используйте правильное имя сервиса: `vibe-team` (не `psb-recommendation`, это имя контейнера)
  
- **Ошибка "No such file or directory":** 
  - Пересоберите образ: `docker-compose build`
  - Убедитесь, что скрипт `scripts/train_model_with_yandexgpt.py` существует
  
- **Модель не сохраняется:** 
  - Проверьте, что volume `./models:/app/models` правильно смонтирован в `docker-compose.yml`
  - Убедитесь, что директория `./models` существует на хосте

**Примечание:** Обучение требует API ключ YandexGPT. Если модель не обучена, система использует улучшенный fallback-алгоритм с анализом графов и паттернов.

### Улучшенный Fallback-алгоритм

Система включает улучшенный fallback-алгоритм, который работает даже без обученной модели. Алгоритм основан на лучших практиках рекомендательных систем и использует нормализацию признаков вместо абсолютных порогов.

**Нормализация признаков:**
- Все метрики нормализуются к шкале 0-1 для более точных оценок
- Используются относительные значения вместо абсолютных порогов
- Композитные индексы: `high_value_customer`, `research_behavior`, `active_spender`

**Адаптивные веса:**
- Автоматически подстраиваются в зависимости от доступности данных:
  - **Все данные доступны:** 40% базовые метрики, 35% анализ графа, 25% паттерны
  - **Только граф:** 50% базовые метрики, 50% анализ графа
  - **Только паттерны:** 60% базовые метрики, 40% паттерны
  - **Только базовые метрики:** 100% базовые метрики

**Улучшения анализа графа:**
- **PageRank** для определения важности узлов
- Анализ категорий и брендов из графа
- Анализ структуры путей (длина, количество)
- Анализ плотности и кластеризации
- Анализ весов рёбер (частоты взаимодействий)

**Улучшения анализа паттернов:**
- Анализ частоты типов событий (просмотры, платежи, клики)
- Анализ последовательностей (сложные паттерны принятия решений)
- Анализ разнообразия паттернов
- Определение паттернов быстрых решений vs. длительного исследования

**Использование категорий брендов:**
- **Ипотека**: категории недвижимости, ремонта, строительства, мебели, интерьера (+0.3-0.45)
- **Кредитная карта**: розничная торговля, услуги, развлечения, рестораны (+0.25-0.3)
- **Вклад**: финансовые услуги, инвестиции, банк, страхование (+0.3-0.35)
- **Кредит**: техника, авто, электроника, бытовая техника (+0.25)

**Использование embedding (опционально):**
- Высокое разнообразие embedding → разнообразные интересы → может нужен кредит для разных покупок
- Учитывается в fallback-алгоритме для улучшения рекомендаций
- Embedding загружается только для товаров пользователя (экономия памяти)

**Улучшенные правила для продуктов:**
- Комбинации признаков вместо простых порогов
- Учет временных паттернов и регулярности активности
- Более точные эвристики на основе комбинаций метрик

---

## Оптимизация для экономии токенов YandexGPT

Система оптимизирована для минимизации использования токенов при работе с YandexGPT:

### Оптимизация графов:

**Агрессивное ограничение данных:**
- Максимум 15 узлов вместо всех (выбираются топ по важности)
- Топ 5 элементов вместо 10
- Топ 5 связей вместо 10
- Топ 5 паттернов вместо 10
- Максимум 2 пути для анализа вместо 3

**Компактное описание:**
- Сокращенные форматы: `Кат123:5` вместо `Категория 123 (5 событий)`
- Агрегация данных: категории и бренды группируются с суммами
- Упрощенные типы: `i→b(w:3)` вместо `item -> brand (сила: 3)`
- Убрана избыточная информация

**Оптимизированные промпты:**
- Сокращенные инструкции
- Убраны лишние слова
- Компактный формат запросов

**Пример оптимизации:**

*До оптимизации (~200 токенов):*
```
Ключевые паттерны поведения пользователя:

Активность по типам:
- item: 25 событий
- brand: 15 событий

Топ активные элементы (20):
1. Категория 123 (8 связей)
2. Категория 456 (6 связей)
...
```

*После оптимизации (~50 токенов):*
```
Поведение:
Типы: item:25, brand:15
Топ категории: Кат123:8, Кат456:6, Кат789:4
Топ бренды: Бр101:$500, Бр202:$300
Последовательности:
1. i→b(w:3)
2. b→i(w:2)
Стат: узлов=40, связей=60, степень=3.0
```

**Экономия токенов:** ~70-80% по сравнению с полным описанием

### Оптимизация на этапе построения графа:

- Топ 20 категорий вместо всех событий
- Топ 15 брендов вместо всех платежей
- Группировка событий по категориям/брендам
- Агрегация данных перед построением графа

### Результат:

- **Быстрее обработка:** меньше данных = быстрее ответ YandexGPT
- **Меньше затрат:** экономия токенов = меньше стоимость API
- **Лучше качество:** фокус на важных данных = более точные рекомендации

## Дополнительные материалы

- [Документация Polars](https://pola-rs.github.io/polars-book/)
- [NetworkX Tutorial](https://networkx.org/documentation/stable/tutorial.html)
- [Streamlit Docs](https://docs.streamlit.io/)
- [LightFM Guide](https://making.lyst.com/lightfm/docs/home.html)
- [Публичная ссылка на датасет](https://disk.yandex.ru/d/H0ZTzS55GSz1Wg)

---

## Команда

Проект разработан командой **VIBE-TEAM** (3 человека) для хакатона ПСБ.

---

## Лицензия

Внутренний проект для ПСБ.

---

## Известные ограничения

- Модель обучается на выборке (не real-time обучение)
- YandexGPT требует API ключ (есть fallback на шаблоны)
- Анализ графов через YandexGPT может занять время при больших графах
- Визуализация графов работает только в браузере (Streamlit)
